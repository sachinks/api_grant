[pytest]
# Tell pytest where to find tests
testpaths = tests

# Add project root to sys.path so all imports resolve without manual path hacks
pythonpath = .

# Show short traceback by default (use --tb=long for full detail)
addopts = --tb=short
